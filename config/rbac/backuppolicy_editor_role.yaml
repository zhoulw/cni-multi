# This rule is not used by the project cni-multi itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the multiapp.zlw.domain.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: cni-multi
    app.kubernetes.io/managed-by: kustomize
  name: backuppolicy-editor-role
rules:
- apiGroups:
  - multiapp.zlw.domain
  resources:
  - backuppolicies
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - multiapp.zlw.domain
  resources:
  - backuppolicies/status
  verbs:
  - get
