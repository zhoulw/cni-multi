apiVersion: multiapp.zlw.domain/v0.1
kind: BackupPolicy
metadata:
  labels:
    app.kubernetes.io/name: cni-multi
    app.kubernetes.io/managed-by: kustomize
  name: backuppolicy-sample
spec:
  # TODO(user): Add fields here
